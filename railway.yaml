build:
  builder: NIXPACKS

deploy:
  startCommand: python manage.py migrate --noinput && python manage.py collectstatic --noinput && daphne -b 0.0.0.0 -p $PORT taxi_project.asgi:application
  restartPolicyType: ON_FAILURE
  restartPolicyMaxRetries: 10
